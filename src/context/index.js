/**
 * @file context/index.js
 * @description Barrel export for all app contexts.
 *
 * Usage:
 *   import { AuthProvider, useAuth } from '../context';
 */

export { default as AuthContext, useAuth, AuthProvider } from './AuthContext';
